import 'package:flutter/widgets.dart';

class Constants {
  static const BorderRadius defauldTfBorderRadius = BorderRadius.all(
    Radius.circular(10),
  );

  static const anaSayfaTextStyle = TextStyle(
    fontSize: 20,
    fontWeight: FontWeight.bold,
  );
}
